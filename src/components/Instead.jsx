/* eslint-disable react/prop-types */
export default function Instead ({children}) {
    return(
        <div className="info">
              Already have an account?{children} instead.
        </div>
        
    );
}